name: Build and publish multi-arch Docker image to Docker Hub

on:
  workflow_dispatch:
  push:
    branches:
      - jawafdehi-main
    tags:
      - v*

env:
  DOCKERHUB_IMAGE: jawafdehi/open-webui

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.DOCKERHUB_IMAGE }}
          tags: |
            type=ref,event=branch
            type=ref,event=tag
            type=sha,prefix=git-

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          platforms: linux/amd64,linux/arm64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            BUILD_HASH=${{ github.sha }}
      - name: Trigger Dokploy redeploy
        if: github.ref == 'refs/heads/jawafdehi-main'
        run: |
          curl -s -X POST \
            -H "x-api-key: ${{ secrets.DOKPLOY_API_KEY }}" \
            -H "Content-Type: application/json" \
            "https://infra.jawafdehi.org/api/compose.deploy" \
            -d '{"composeId":"cnL6qss7fOjk-g2tlp4Ua","title":"Auto-deploy: ${{ github.sha }}"}'
